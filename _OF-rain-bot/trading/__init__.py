"""Canonical trading package for STANDARTS-aligned architecture.

This package is introduced as the migration target for the current
`orderflow` implementation. Runtime behavior still lives in the legacy
modules until subsequent refactoring phases move code behind these
boundaries.
"""

