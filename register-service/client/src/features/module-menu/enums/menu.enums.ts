export enum MenuTypes {
    CUSTOMER = 'customer',
    GUEST = 'guest',
}

export enum MenuPlacement {
    PRIMARY = 'primary',
    SECONDARY = 'secondary',
    BOTTOM = 'bottom',
    ACTION = 'action',
}
