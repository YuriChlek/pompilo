"""Platform-native trading bot module packages.

This package is the discovery boundary for signal-only bot modules. Child packages
should expose lightweight manifest and config-schema metadata without importing strategy
runtime code during discovery.
"""

